Prerequisites
=============

 - ncurses
 - gcc


Installing
==========

  > ./configure
  > make install
  > echo /usr/local/bin/fish >>/etc/shells

  To set fish as the default shell:
  > chsh -s /usr/local/bin/fish
